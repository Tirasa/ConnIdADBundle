host = localhost
port = 636
principal = cn=Administrator,cn=Users,dc=example,dc=com
credentials = password
usersBaseContext = cn=Users,dc=example,dc=com
baseContextToSynchronize = dc=example,dc=com
#Please provide two groups for the test (memberships in OR)
memberships = CN=Domain Guests,CN=Users,DC=example,DC=com ; CN=Guests,CN=Builtin,DC=example,dc=com
