host = localhost
port = 636
principal = cn=Administrator,cn=Users,dc=example,dc=com
credentials = password
usersBaseContext = dc=example,dc=com
baseContextToSynchronize = dc=example,dc=com
#Please provide two groups for the test (memberships in OR) and do not use HelpServicesGroup group
memberships = CN=Domain Guests,CN=Users,DC=example,DC=com ; CN=Domain Computers,CN=Users,DC=example,dc=com
